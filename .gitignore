# --- Dependencies ---
# Usually huge, specific to the environment, and should be installed, not committed.
node_modules/

# --- Environment Variables ---
# Contains sensitive credentials and environment-specific settings.
.env
.env.*
# Optional: Allow committing an example env file as a template (uncomment if needed)
# !.env.example

# --- Logs ---
# Log files are runtime artifacts and should not be committed.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Build Output / Compiled Files ---
# Generated files from build processes (TypeScript, Webpack, Rollup, etc.).
# Adjust 'dist' or 'build' to your actual output directory if different.
dist/
build/
out/
.parcel-cache/

# --- Test Reports & Coverage ---
# Generated during test runs.
coverage/
junit.xml
*.lcov

# --- OS Generated Files ---
# Files generated by macOS, Windows, Linux that are metadata/caches.
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Editor/IDE Directories and Files ---
# User-specific settings for editors like VS Code, JetBrains IDEs, etc.
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw? # Vim swap files

# --- Optional NPM/Yarn files ---
# Sometimes generated during specific operations or errors.
*.tgz
yarn.lock~ # Yarn lockfile backup

# --- Optional files ---
# Add any other project-specific generated files or temporary directories here
# e.g., temp/, cache/
